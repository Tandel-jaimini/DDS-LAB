#include <iostream>
    using namespace std;
    int gcd (int a,int b)
    {
        if(b==0)
        return a;
        return gcd(b,a%b);
        
    }
    int main() {
        int a=36,b=60;
        int c=gcd(a,b);
        cout<<"GCD of a & b is "<<c<<endl;
        return 0;
    }
